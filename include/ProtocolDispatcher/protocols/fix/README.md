# FIX

This directory is reserved for the future FIX subsystem.

FIX will be added after the dispatch framework, HTTP, and WebSocket layers are stable. It is expected to contain its own parser, serializer, session management, validation, and persistence concerns instead of being treated as a small extension of the HTTP/WebSocket code.
